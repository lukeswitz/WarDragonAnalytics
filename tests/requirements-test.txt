# WarDragon Analytics - Test Dependencies
# Install with: pip install -r tests/requirements-test.txt

# Testing framework
pytest==8.0.0
# Modern testing framework with rich features

pytest-asyncio==0.23.3
# Async test support for pytest
# Required for testing async functions and fixtures

pytest-cov==4.1.0
# Test coverage reporting
# Run with: pytest --cov=app --cov-report=html

pytest-timeout==2.2.0
# Timeout support to prevent hanging tests
# Automatically kills tests that run too long

pytest-xdist==3.5.0
# Parallel test execution
# Run with: pytest -n auto

pytest-mock==3.12.0
# Mocking support for pytest
# Useful for unit tests

# HTTP testing
httpx==0.27.0
# Modern async HTTP client
# Used for API endpoint testing

# Database testing
asyncpg==0.29.0
# Async PostgreSQL driver
# Used for direct database operations in tests

sqlalchemy==2.0.25
# SQL toolkit and ORM
# Used for database setup and teardown

psycopg2-binary==2.9.9
# PostgreSQL adapter
# Required for SQLAlchemy operations

# Configuration
pyyaml==6.0.1
# YAML parser for test configuration

python-dotenv==1.0.0
# Environment variable loading

# Utilities
freezegun==1.4.0
# Time/date mocking for tests
# Useful for testing time-based queries

faker==22.6.0
# Fake data generation
# Useful for generating realistic test data

# Code quality
black==24.1.1
# Code formatter
# Run with: black .

ruff==0.1.14
# Fast Python linter
# Run with: ruff check .

mypy==1.8.0
# Static type checker
# Run with: mypy app/

# Development
ipython==8.20.0
# Enhanced Python shell
# Useful for debugging tests

ipdb==0.13.13
# IPython debugger
# Use breakpoint() in tests for interactive debugging
